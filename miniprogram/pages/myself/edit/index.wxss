/* pages/myself/profile/index.wxss */

/* 页面背景 */
page {
  background-color: #f8f8f8;
}

.container {
  padding: 0 0 40rpx 0;
}

/* --- 顶部头像区域 (已修正为全宽容器，头像居中) --- */
.profile-header {
  display: flex;
  justify-content: center; /* 保持头像居中 */
  background-color: #fff;
  /* 确保和下方区域连接 */
  margin-bottom: 0; 
  width: 100%;
  height: 280rpx;
}
.avatar-wrap {
  position: relative;
  width: 140rpx;
  /* 头像图片尺寸 */
  height: 140rpx; /* 头像图片尺寸 */
  border-radius: 50%;
  margin-top: 70rpx;
}
.avatar {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  border: 1rpx solid #eee;
}
.camera-icon {
  position: absolute;
  bottom: 0;
  right: 0;
  width: 40rpx;
  height: 40rpx;
  line-height: 40rpx;
  background-color: #ff4500;
  border-radius: 50%;
  color: #fff;
  text-align: center;
  font-size: 20rpx;
}

/* --- 表单列表 (与头像区域连接) --- */
.form-list {
  background-color: #fff;
  width: 750rpx;
}
.form-item {
  display: flex;
  align-items: center;
  padding: 30rpx 40rpx;
  border-bottom: 1rpx solid #eee;
}
.form-item:last-child {
  border-bottom: none;
}
.label {
  width: 160rpx;
  font-size: 30rpx;
  color: #333;
  flex-shrink: 0;
}
.input {
  flex-grow: 1;
  text-align: right;
  font-size: 30rpx;
  color: #333;
}
.placeholder {
  color: #999;
}
.value {
  text-align: right;
  font-size: 30rpx;
  color: #333;
}

/* 手机号特殊样式 */
.mobile-item {
  padding-right: 20rpx;
  /* 留出按钮空间 */
}
.mobile-input {
  text-align: left;
  flex-grow: 1;
}
.mobile-bound {
  flex-grow: 1;
  display: flex;
  justify-content: space-between;
  align-items: center;
  padding-right: 20rpx; /* 避免手机号贴着右侧 */
}
.mobile-bound text {
  font-size: 30rpx;
  color: #333;
}

/* !!! 🚀 更换手机号按钮 - 镂空样式 !!!
*/
.btn-change-mobile {
  width: 250rpx !important; 
  height: 60rpx;  
  line-height: 58rpx; /* 调整 line-height 适应边框 */
  padding: 0 30rpx;
  font-size: 28rpx;
  background-color: #fff; /* 镂空 */
  color: #ff4500; /* 主题色文字 */
  border: 1rpx solid #ff4500;
  /* 镂空边框 */
  border-radius: 8rpx;
  margin-left: 100rpx !important;
  flex-shrink: 0;
}

/* !!! 🚀 发送短信按钮 - 镂空样式 !!!
*/
.btn-send-sms {
  width: 250rpx !important; 
  height: 60rpx;  
  line-height: 58rpx; /* 调整 line-height 适应边框 */
  padding: 0 30rpx;
  font-size: 28rpx;
  background-color: #fff; /* 镂空 */
  color: #ff4500; /* 主题色文字 */
  border: 1rpx solid #ff4500;
  /* 镂空边框 */
  border-radius: 8rpx;
  margin-left: 20rpx;
  flex-shrink: 0;
}
.btn-send-sms[disabled] {
  background-color: #fff;
  /* 保持镂空 */
  color: #ccc; /* 禁用文字颜色 */
  border: 1rpx solid #ccc;
  /* 禁用边框颜色 */
}

/* 性别单选 */
.radio-group-wrap {
  flex-grow: 1;
  text-align: right;
}
.radio-label {
  margin-left: 30rpx;
  font-size: 30rpx;
  color: #333;
}

/* 生日选择器链接箭头 */
.item-link .value {
  margin-right: 10rpx;
}
.item-link::after {
  content: '>';
  color: #999;
  font-size: 30rpx;
  margin-left: auto;
}

/* --- 底部按钮 --- */
.btn-group {
  padding: 40rpx;
  margin-top: 10rpx;
}
.btn-save {
  background-color: #ff4500;
  color: #fff;
  width: 720rpx !important;
}

/* 【新增】保存按钮禁用样式 */
.btn-save[disabled] {
  background-color: #ccc; /* 禁用时的背景色 */
  color: #f8f8f8; /* 禁用时的文字颜色 */
}

.btn-logout {
  background-color: #fff;
  color: #ff4500;
  border: 1rpx solid #ff4500;
}